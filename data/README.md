# Data

This folder stores generated synthetic ICU datasets.

Generate data with:

```powershell
python -m icu_mortality_insight.data --output data/icu_synthetic.csv --rows 6000
```

Do not commit real patient data or protected health information.

